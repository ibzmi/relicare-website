r · HTML
Copy

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>What is UK Packaging EPR? | Relicare</title>
  <meta name="description" content="A plain-English guide to Extended Producer Responsibility for packaging. Who is in scope, what small producers must do, fees, deadlines, and what you don't have to do." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet" />

  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --ink:        #0f1a14;
      --ink-muted:  #4a5c50;
      --ink-faint:  #8a9e90;
      --paper:      #f5f2eb;
      --paper-warm: #ede9df;
      --green:      #1a6640;
      --green-light:#2d8a58;
      --green-pale: #e8f2ec;
      --accent:     #c8602a;
      --border:     #d4cec2;
      --white:      #ffffff;
    }

    html { scroll-behavior: smooth; }
    body {
      font-family: 'DM Sans', sans-serif;
      background: var(--paper);
      color: var(--ink);
      font-size: 16px;
      line-height: 1.6;
      overflow-x: hidden;
    }

    /* ── NAV (identical to homepage) ── */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      background: rgba(245,242,235,0.92);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border);
      padding: 0 2rem;
      height: 64px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .nav-logo {
      font-family: 'DM Serif Display', serif;
      font-size: 1.5rem;
      color: var(--green);
      text-decoration: none;
      letter-spacing: -0.02em;
    }
    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
      align-items: center;
    }
    .nav-links a {
      text-decoration: none;
      color: var(--ink-muted);
      font-size: 0.9rem;
      font-weight: 500;
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--green); }
    .nav-links a.active { color: var(--green); font-weight: 600; }
    .nav-cta {
      background: var(--green) !important;
      color: var(--white) !important;
      padding: 0.5rem 1.2rem;
      border-radius: 4px;
      font-weight: 600 !important;
      transition: background 0.2s !important;
    }
    .nav-cta:hover { background: var(--green-light) !important; }

    /* ── SHARED ── */
    section { padding: 5rem 2rem; }
    .container { max-width: 1100px; margin: 0 auto; }

    .section-label {
      font-size: 0.72rem;
      font-weight: 600;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--green);
      margin-bottom: 0.75rem;
    }
    .section-title {
      font-family: 'DM Serif Display', serif;
      font-size: clamp(1.8rem, 3vw, 2.6rem);
      letter-spacing: -0.02em;
      line-height: 1.2;
      color: var(--ink);
      margin-bottom: 1rem;
    }
    .section-intro {
      font-size: 1.05rem;
      color: var(--ink-muted);
      max-width: 640px;
      line-height: 1.7;
      font-weight: 300;
    }

    /* ── BUTTONS (identical to homepage) ── */
    .btn-primary {
      background: var(--green);
      color: var(--white);
      padding: 0.85rem 1.8rem;
      border-radius: 4px;
      text-decoration: none;
      font-weight: 600;
      font-size: 0.95rem;
      border: 2px solid var(--green);
      transition: background 0.2s, transform 0.15s;
      display: inline-block;
    }
    .btn-primary:hover { background: var(--green-light); border-color: var(--green-light); transform: translateY(-1px); }
    .btn-secondary {
      background: transparent;
      color: var(--ink);
      padding: 0.85rem 1.8rem;
      border-radius: 4px;
      text-decoration: none;
      font-weight: 500;
      font-size: 0.95rem;
      border: 2px solid var(--border);
      transition: border-color 0.2s, color 0.2s;
      display: inline-block;
    }
    .btn-secondary:hover { border-color: var(--ink-muted); color: var(--green); }
    .btn-primary-light {
      background: var(--paper);
      color: var(--ink);
      padding: 0.85rem 1.8rem;
      border-radius: 4px;
      text-decoration: none;
      font-weight: 600;
      font-size: 0.95rem;
      border: 2px solid var(--paper);
      transition: background 0.2s;
      display: inline-block;
    }
    .btn-primary-light:hover { background: var(--paper-warm); }
    .btn-ghost {
      background: transparent;
      color: #8a9e90;
      padding: 0.85rem 1.8rem;
      border-radius: 4px;
      text-decoration: none;
      font-weight: 500;
      font-size: 0.95rem;
      border: 2px solid rgba(255,255,255,0.15);
      transition: border-color 0.2s, color 0.2s;
      display: inline-block;
    }
    .btn-ghost:hover { border-color: rgba(255,255,255,0.35); color: var(--paper); }

    /* ── PAGE HERO ── */
    .page-hero {
      padding-top: 120px;
      padding-bottom: 3rem;
      padding-left: 2rem;
      padding-right: 2rem;
      background: var(--paper);
    }
    .page-hero-inner {
      max-width: 1100px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 340px;
      gap: 4rem;
      align-items: start;
    }
    .hero-label {
      display: inline-block;
      font-size: 0.75rem;
      font-weight: 600;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--green);
      background: var(--green-pale);
      border: 1px solid #b8dcc4;
      padding: 0.3rem 0.8rem;
      border-radius: 2px;
      margin-bottom: 1.5rem;
    }
    .page-hero h1 {
      font-family: 'DM Serif Display', serif;
      font-size: clamp(2.4rem, 4vw, 3.4rem);
      line-height: 1.1;
      letter-spacing: -0.03em;
      color: var(--ink);
      margin-bottom: 1.25rem;
    }
    .page-hero .hero-sub {
      font-size: 1.05rem;
      color: var(--ink-muted);
      line-height: 1.7;
      font-weight: 300;
      max-width: 560px;
      margin-bottom: 0.75rem;
    }
    .hero-disclaimer {
      font-size: 0.78rem;
      color: var(--ink-faint);
      margin-top: 0.75rem;
    }

    /* TOC CARD — styled like hero-card on homepage */
    .toc-card {
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 1.75rem;
      box-shadow: 0 4px 24px rgba(15,26,20,0.08);
      position: sticky;
      top: 80px;
      opacity: 0;
      transform: translateY(16px);
      animation: fadeUp 0.6s ease 0.2s forwards;
    }
    @keyframes fadeUp { to { opacity: 1; transform: translateY(0); } }
    .toc-card-header {
      font-size: 0.7rem;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--ink-faint);
      margin-bottom: 1rem;
      padding-bottom: 0.75rem;
      border-bottom: 1px solid var(--border);
    }
    .toc-list { list-style: none; display: flex; flex-direction: column; gap: 0.1rem; }
    .toc-list li a {
      display: flex;
      align-items: baseline;
      gap: 0.6rem;
      padding: 0.35rem 0.5rem;
      border-radius: 3px;
      font-size: 0.82rem;
      font-weight: 500;
      color: var(--ink-muted);
      text-decoration: none;
      transition: background 0.15s, color 0.15s;
      line-height: 1.4;
    }
    .toc-list li a:hover { background: var(--green-pale); color: var(--green); }
    .toc-list li a.toc-active { background: var(--green-pale); color: var(--green); font-weight: 600; }
    .toc-list li a .toc-num {
      font-size: 0.7rem;
      color: var(--ink-faint);
      flex-shrink: 0;
      min-width: 14px;
    }
    .toc-list li a.toc-active .toc-num { color: var(--green); }

    /* ── TRUST BAR (identical to homepage) ── */
    .trust-bar {
      background: var(--ink);
      color: #a0b4a8;
      padding: 1rem 2rem;
      text-align: center;
      font-size: 0.85rem;
      letter-spacing: 0.02em;
    }
    .trust-bar strong { color: var(--paper); }

    /* ── CONTENT LAYOUT ── */
    .content-outer {
      padding: 0 2rem 5rem;
    }
    .content-grid {
      max-width: 1100px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 340px;
      gap: 4rem;
      align-items: start;
    }

    /* ── ARTICLE ── */
    .article { min-width: 0; }

    .art-section {
      padding: 3rem 0;
      border-bottom: 1px solid var(--border);
      opacity: 0;
      transform: translateY(12px);
      animation: fadeUp 0.5s ease forwards;
    }
    .art-section:last-child { border-bottom: none; }
    .art-section:nth-child(1) { animation-delay: 0.05s; }
    .art-section:nth-child(2) { animation-delay: 0.10s; }
    .art-section:nth-child(3) { animation-delay: 0.15s; }
    .art-section:nth-child(4) { animation-delay: 0.20s; }
    .art-section:nth-child(5) { animation-delay: 0.25s; }
    .art-section:nth-child(6) { animation-delay: 0.30s; }
    .art-section:nth-child(7) { animation-delay: 0.35s; }
    .art-section:nth-child(8) { animation-delay: 0.40s; }
    .art-section:nth-child(9) { animation-delay: 0.45s; }
    .art-section:nth-child(10) { animation-delay: 0.50s; }

    .art-section-num {
      display: inline-block;
      font-size: 0.7rem;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--green);
      background: var(--green-pale);
      border: 1px solid #b8dcc4;
      padding: 0.2rem 0.6rem;
      border-radius: 2px;
      margin-bottom: 0.75rem;
    }

    .art-section h2 {
      font-family: 'DM Serif Display', serif;
      font-size: clamp(1.4rem, 2.5vw, 1.9rem);
      letter-spacing: -0.02em;
      color: var(--ink);
      margin-bottom: 1.1rem;
      scroll-margin-top: 88px;
    }

    .art-section p {
      font-size: 0.9375rem;
      color: var(--ink-muted);
      line-height: 1.7;
      margin-bottom: 1rem;
      font-weight: 300;
    }
    .art-section p strong { color: var(--ink); font-weight: 600; }
    .art-section p:last-child { margin-bottom: 0; }

    .art-section ul {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      margin: 0.75rem 0 1rem;
    }
    .art-section ul li {
      position: relative;
      padding-left: 1.25rem; /* Creates space for the bullet */
      font-size: 0.9rem;
      color: var(--ink-muted);
      line-height: 1.6;
      font-weight: 300;
      display: block; /* Prevents the text from splitting into columns */
    }
    .art-section ul li::before {
      content: '';
      position: absolute;
      left: 0;
      top: 10px; /* Vertically aligns the bullet with the first line of text */
      width: 6px; 
      height: 6px;
      background: var(--green);
      border-radius: 50%;
    }
    .art-section ul li strong { color: var(--ink); font-weight: 600; }

    /* ── CALLOUT BOXES (using problem-card / qa-guarantee patterns) ── */
    .callout {
      border-radius: 6px;
      padding: 1.25rem 1.5rem;
      margin: 1.5rem 0;
      border-left: 3px solid;
    }
    .callout-lbl {
      font-size: 0.68rem;
      font-weight: 600;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      margin-bottom: 0.4rem;
    }
    .callout p {
      font-size: 0.875rem !important;
      margin-bottom: 0 !important;
      line-height: 1.65 !important;
      font-weight: 400 !important;
    }
    .callout-warn { background: #fffbeb; border-color: #d97706; }
    .callout-warn .callout-lbl { color: #92400e; }
    .callout-warn p { color: #78350f !important; }
    .callout-info { background: var(--green-pale); border-color: var(--green); }
    .callout-info .callout-lbl { color: var(--green); }
    .callout-info p { color: var(--green) !important; }

    /* ── STEP LIST (from process-section on homepage) ── */
    .step-list { margin: 1.5rem 0; }
    .process-step {
      display: grid;
      grid-template-columns: 60px 1fr;
      gap: 1.25rem;
    }
    .step-number-col { display: flex; flex-direction: column; align-items: center; }
    .step-num {
      width: 44px; height: 44px;
      background: var(--green); color: var(--white);
      border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      font-weight: 600; font-size: 0.85rem; flex-shrink: 0;
    }
    .step-line { width: 2px; flex: 1; background: var(--border); margin-top: 6px; }
    .process-step:last-child .step-line { display: none; }
    .step-content { padding-bottom: 2rem; }
    .process-step:last-child .step-content { padding-bottom: 0; }
    .step-title { font-weight: 600; font-size: 0.975rem; color: var(--ink); margin-bottom: 0.4rem; }
    .step-desc { font-size: 0.875rem; color: var(--ink-muted); line-height: 1.65; font-weight: 300; }
    .step-outcome {
      margin-top: 0.65rem;
      padding: 0.55rem 0.85rem;
      background: var(--green-pale);
      border-left: 3px solid var(--green);
      border-radius: 0 4px 4px 0;
      font-size: 0.8rem;
      color: var(--green);
      font-weight: 500;
    }

    /* ── TABLE ── */
    .table-wrap {
      overflow-x: auto;
      margin: 1.25rem 0;
      border: 1px solid var(--border);
      border-radius: 6px;
    }
    table { width: 100%; border-collapse: collapse; font-size: 0.875rem; }
    thead th {
      background: var(--ink);
      color: var(--paper);
      padding: 0.7rem 1rem;
      text-align: left;
      font-weight: 600;
      font-size: 0.8rem;
      letter-spacing: 0.02em;
    }
    tbody tr:nth-child(odd)  { background: var(--white); }
    tbody tr:nth-child(even) { background: var(--paper); }
    tbody td {
      padding: 0.7rem 1rem;
      color: var(--ink-muted);
      border-top: 1px solid var(--border);
      vertical-align: top;
      font-size: 0.875rem;
      font-weight: 300;
    }
    tbody td strong { color: var(--ink); font-weight: 600; }

    /* ── CODE TAG ── */
    .tag {
      display: inline-block;
      font-family: 'SF Mono', 'Fira Code', monospace;
      font-size: 0.78rem;
      font-weight: 600;
      background: var(--green-pale);
      color: var(--green);
      border: 1px solid #b8dcc4;
      padding: 0.1rem 0.45rem;
      border-radius: 3px;
    }

    /* ── STICKY SIDEBAR (desktop only) ── */
    .sidebar { 
      min-width: 0; 
      margin-top: 3rem; /* This pushes the box down to perfectly align with the article text */
    }
    .sidebar-sticky { 
      position: sticky; 
      top: 80px; 
      display: flex; 
      flex-direction: column; 
      gap: 1.5rem; 
    }

    /* ── SIDEBAR CTA CARD ── */
    .cta-card {
      background: var(--ink);
      border-radius: 6px;
      padding: 1.75rem;
      color: var(--paper);
    }
    .cta-card .section-label { color: #6daa84; }
    .cta-card h3 {
      font-family: 'DM Serif Display', serif;
      font-size: 1.25rem;
      color: var(--paper);
      margin-bottom: 0.75rem;
      line-height: 1.25;
    }
    .cta-card p { font-size: 0.85rem; color: #8a9e90; line-height: 1.6; margin-bottom: 1.25rem; font-weight: 300; }

    /* ── SOURCES ── */
    .sources-list { list-style: none; display: flex; flex-direction: column; gap: 0; }
    .sources-list li {
      display: flex;
      gap: 0.5rem;
      align-items: baseline;
      font-size: 0.8rem;
      color: var(--ink-faint);
      padding: 0.5rem 0;
      border-bottom: 1px solid var(--border);
      font-weight: 300;
      line-height: 1.5;
    }
    .sources-list li:last-child { border-bottom: none; }
    .sources-list li::before { content: '↗'; color: var(--green); font-size: 0.7rem; flex-shrink: 0; margin-top: 2px; }

    /* ── CTA SECTION (identical to homepage cta-section) ── */
    .cta-section { background: var(--ink); text-align: center; padding: 6rem 2rem; }
    .cta-section .section-label { color: #6daa84; }
    .cta-section h2 {
      font-family: 'DM Serif Display', serif;
      font-size: clamp(2rem, 4vw, 3.2rem);
      color: var(--paper);
      letter-spacing: -0.02em;
      margin-bottom: 1rem;
      line-height: 1.2;
    }
    .cta-section p { font-size: 1.05rem; color: #8a9e90; max-width: 520px; margin: 0 auto 2.5rem; line-height: 1.7; }
    .cta-actions { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }

    /* ── FOOTER (identical to homepage) ── */
    footer { background: var(--ink); border-top: 1px solid rgba(255,255,255,0.06); padding: 2rem; text-align: center; }
    footer p { font-size: 0.8rem; color: #4a5c50; line-height: 1.8; }
    footer a { color: #6daa84; text-decoration: none; }
    footer a:hover { text-decoration: underline; }

    /* ── RESPONSIVE ── */
    @media (max-width: 900px) {
      .page-hero-inner { grid-template-columns: 1fr; }
      .toc-card { display: none; }
      .content-grid { grid-template-columns: 1fr; }
      .sidebar { display: none; }
      .nav-links { display: none; }
    }
    @media (max-width: 600px) {
      section { padding: 3.5rem 1.25rem; }
      .process-step { grid-template-columns: 44px 1fr; gap: 1rem; }
    }
  </style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="/" class="nav-logo">Relicare</a>
  <ul class="nav-links">
    <li><a href="/#how-it-works">How it works</a></li>
    <li><a href="/#packages">Pricing</a></li>
    <li><a href="/explainer" class="active">What is EPR?</a></li>
    <li><a href="/eligibility-check" class="nav-cta">Check eligibility</a></li>
  </ul>
</nav>

<!-- PAGE HERO -->
<div class="page-hero">
  <div class="page-hero-inner">
    <div>
      <div class="hero-label">UK Packaging EPR — Plain-English Guide</div>
      <h1>Extended Producer Responsibility for Packaging</h1>
      <p class="hero-sub">If your organisation supplies packaging into the UK market, EPR can create legal duties to register, pay a fee, and collect and report data. This page explains exactly what small producers need to know — using official government guidance only.</p>
      <p class="hero-disclaimer">Based on GOV.UK guidance current as of early 2026. Not legal advice. Always verify deadlines and fees with your environmental regulator.</p>
    </div>

    <!-- TOC CARD -->
    <div class="toc-card">
      <div class="toc-card-header">On this page</div>
      <ol class="toc-list">
        <li><a href="#scope"><span class="toc-num">1.</span> Are you in scope?</a></li>
        <li><a href="#thresholds"><span class="toc-num">2.</span> Small vs large producer</a></li>
        <li><a href="#activities"><span class="toc-num">3.</span> Packaging activities</a></li>
        <li><a href="#steps"><span class="toc-num">4.</span> What you must do</a></li>
        <li><a href="#rules"><span class="toc-num">5.</span> Reporting rules</a></li>
        <li><a href="#bags"><span class="toc-num">6.</span> Plastic &amp; paper bags</a></li>
        <li><a href="#fees"><span class="toc-num">7.</span> Fees</a></li>
        <li><a href="#dont"><span class="toc-num">8.</span> What you don't have to do</a></li>
        <li><a href="#consequences"><span class="toc-num">9.</span> Consequences</a></li>
        <li><a href="#sources"><span class="toc-num">—</span> Sources</a></li>
      </ol>
    </div>
  </div>
</div>

<!-- TRUST BAR -->
<div class="trust-bar">
  Written for <strong>UK small producers</strong> — D2C brands, importers, food &amp; drink, cosmetics, home goods. All claims sourced from official GOV.UK guidance.
</div>

<!-- CONTENT -->
<div class="content-outer">
  <div class="content-grid">

    <!-- ARTICLE -->
    <article class="article">

      <!-- 1. SCOPE -->
      <div class="art-section" id="scope">
        <div class="art-section-num">Section 1</div>
        <h2>Quick test: are you in scope at all?</h2>
        <p>Government guidance says you need to collect and report packaging data for a given year if <strong>all</strong> of these apply:</p>
        <ul>
          <li>You are an individual business, subsidiary, or group — and you are <strong>not a charity</strong> for the purposes of producer obligations.</li>
          <li>Your annual turnover is <strong>£1 million or more</strong>.</li>
          <li>You were responsible for supplying or importing <strong>more than 25 tonnes</strong> of packaging in the previous calendar year.</li>
          <li>You carry out at least one <a href="#activities" style="color:var(--green);font-weight:500;">packaging activity</a> (see Section 3).</li>
        </ul>
        <p>If you fail any of those tests, you may be out of scope for that year — but re-check each year as turnover and packaging amounts change.</p>
        <div class="callout callout-info">
          <div class="callout-lbl">Note on charities</div>
          <p>GOV.UK explains that charities are exempt from obligations and fees as packaging producers, but different rules can still apply in other roles — for example, if acting as a reprocessor or exporter.</p>
        </div>
      </div>

      <!-- 2. THRESHOLDS -->
      <div class="art-section" id="thresholds">
        <div class="art-section-num">Section 2</div>
        <h2>Small producer vs large producer</h2>
        <p>Government guidance defines a <strong>small producer</strong> as an organisation that either:</p>
        <ul>
          <li>Has annual turnover of more than £1 million and up to £2 million and supplies more than 25 tonnes of packaging in the UK, <strong>or</strong></li>
          <li>Has annual turnover of more than £1 million and supplies more than 25 tonnes and no more than 50 tonnes of packaging in the UK.</li>
        </ul>
        <p>If your turnover is <strong>£2 million or more</strong> and you supply more than 50 tonnes, you are a <strong>large producer</strong> — reporting is bi-annual, and waste disposal fees and PRN obligations apply.</p>
        <div class="callout callout-warn">
          <div class="callout-lbl">Common misconception</div>
          <p>"Small" is not about how many people you employ. Classification is based purely on financial turnover and packaging tonnage.</p>
        </div>
      </div>

      <!-- 3. ACTIVITIES -->
      <div class="art-section" id="activities">
        <div class="art-section-num">Section 3</div>
        <h2>What counts as a packaging activity?</h2>
        <p>Government guidance lists six activities that can trigger an obligation. You only need to carry out <em>one</em> of these to be in scope:</p>
        <ul>
          <li>Supplying packaged goods to the UK market under your own brand</li>
          <li>Packing or filling packaging</li>
          <li>Importing products in packaging</li>
          <li>Supplying empty packaging</li>
          <li>Hiring or loaning out reusable packaging</li>
          <li>Operating an online marketplace where non-UK sellers sell into the UK</li>
        </ul>
      </div>

      <!-- 4. STEPS -->
      <div class="art-section" id="steps">
        <div class="art-section-num">Section 4</div>
        <h2>What small producers must do — the real-world steps</h2>

        <div class="step-list">
          <div class="process-step">
            <div class="step-number-col">
              <div class="step-num">1</div>
              <div class="step-line"></div>
            </div>
            <div class="step-content">
              <div class="step-title">Create an account on the Report Packaging Data service</div>
              <div class="step-desc">Only certain roles can create the account: a director, company secretary, partner, limited liability partnership member, or sole trader. The environmental regulator must approve your account before you can submit data. <strong>GOV.UK says this can take up to 28 days</strong> — do not leave it until the week of the deadline.</div>
            </div>
          </div>
          <div class="process-step">
            <div class="step-number-col">
              <div class="step-num">2</div>
              <div class="step-line"></div>
            </div>
            <div class="step-content">
              <div class="step-title">Register your organisation and pay the fee</div>
              <div class="step-desc">Small organisations must register for the 2026 compliance year by April 2026. GOV.UK states that regulators will not review a registration application until you have submitted your first packaging data <em>and</em> your registration data <em>and</em> paid the fee. All three must be in place before the application is processed.</div>
            </div>
          </div>
          <div class="process-step">
            <div class="step-number-col">
              <div class="step-num">3</div>
              <div class="step-line"></div>
            </div>
            <div class="step-content">
              <div class="step-title">Collect the data you must report</div>
              <div class="step-desc">Collect data about your packaging activities, the class (layer) of packaging, the packaging material, and the weight of each material in kilograms. Also collect data on plastic and paper bags supplied in England where relevant. <strong>Keep your data and the evidence for it for at least 7 years.</strong></div>
            </div>
          </div>
          <div class="process-step">
            <div class="step-number-col">
              <div class="step-num">4</div>
            </div>
            <div class="step-content">
              <div class="step-title">Report your packaging data — yearly, by 1 April</div>
              <div class="step-desc">Small producers report once a year. Submit January–December data by 1 April the following year. January–December 2025 data is due by 1 April 2026. If you miss a deadline, official guidance says to contact your environmental regulator directly.</div>
              <div class="step-outcome">✓ Deadline: 1 April 2026 for 2025 calendar year data</div>
            </div>
          </div>
        </div>
      </div>

      <!-- 5. RULES -->
      <div class="art-section" id="rules">
        <div class="art-section-num">Section 5</div>
        <h2>The reporting rules you must not get wrong</h2>
        <p>Government guidance says small producers must:</p>
        <ul>
          <li>Record all drinks containers as <strong>household drinks containers</strong></li>
          <li>Report plastic simply as <strong>"plastic"</strong> — do not split into rigid and flexible</li>
          <li>Only use the codes <span class="tag">SP</span> (small organisation packaging — all) and <span class="tag">HDC</span> (household drinks containers)</li>
        </ul>

        <div class="table-wrap">
          <table>
            <thead>
              <tr><th>Code</th><th>What it covers</th></tr>
            </thead>
            <tbody>
              <tr>
                <td><span class="tag">SP</span></td>
                <td>All packaging you are responsible for — all classes and all activities</td>
              </tr>
              <tr>
                <td><span class="tag">HDC</span></td>
                <td>All drinks containers — must be recorded as HDC, not SP</td>
              </tr>
            </tbody>
          </table>
        </div>

        <p>GOV.UK file specifications state that packaging material weight must be entered using <strong>digits only, in whole numbers (kilograms)</strong>. If your file fails portal validation it will not upload — you must correct and re-upload.</p>

        <div class="callout callout-warn">
          <div class="callout-lbl">CSV only — not Excel</div>
          <p>The portal will not accept .xls or .xlsx files. Your file must be a true CSV conforming exactly to the government's file specification. A formatting error means rejection with no mechanism to fix it after the deadline.</p>
        </div>
      </div>

      <!-- 6. BAGS -->
      <div class="art-section" id="bags">
        <div class="art-section-num">Section 6</div>
        <h2>Plastic and paper bags supplied in England</h2>
        <p>Government guidance states the <strong>first mandatory report</strong> for plastic and paper bags supplied in England is for <strong>2026 data, due by 1 April 2027</strong>.</p>
        <p>It also points you to the Environment Agency's Regulatory Position Statement (RPS 330) about collecting and submitting 2025 plastic and paper bag data. If bags are relevant to your business, read and follow that statement before your next submission.</p>
      </div>

      <!-- 7. FEES -->
      <div class="art-section" id="fees">
        <div class="art-section-num">Section 7</div>
        <h2>Fees — and the official discrepancy you must not ignore</h2>
        <p>Official GOV.UK pages currently show different fee schedules for small producers:</p>

        <div class="table-wrap">
          <table>
            <thead>
              <tr>
                <th>GOV.UK page</th>
                <th>Direct registration</th>
                <th>Via compliance scheme</th>
                <th>Late fee</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td><strong>"Register and pay the fee" guidance</strong></td>
                <td>£1,303</td>
                <td>£696</td>
                <td>£386</td>
              </tr>
              <tr>
                <td><strong>"Small producer" guidance</strong></td>
                <td>£1,216</td>
                <td>£631</td>
                <td>—</td>
              </tr>
            </tbody>
          </table>
        </div>

        <div class="callout callout-warn">
          <div class="callout-lbl">Do not rely on any static figure — including the ones above</div>
          <p>Because official GOV.UK pages currently list different fee amounts, check the fee shown in the <strong>Report Packaging Data service at the time you register</strong> and cross-check the 'register and pay the fee' guidance. Keep a dated screenshot or record of what you were shown.</p>
        </div>
      </div>

      <!-- 8. DON'T HAVE TO -->
      <div class="art-section" id="dont">
        <div class="art-section-num">Section 8</div>
        <h2>What small producers do not have to do</h2>
        <p>Government guidance is explicit. As a small producer you do <strong>not</strong> have to:</p>
        <ul>
          <li>Pay waste disposal fees</li>
          <li>Buy packaging waste recycling notes (PRNs) — the evidence large producers use to meet recycling obligations</li>
        </ul>
        <div class="callout callout-info">
          <div class="callout-lbl">Watch out</div>
          <p>If a service provider quotes you for a large-producer compliance bundle and you are a confirmed small producer, question it. These are materially different obligations.</p>
        </div>
      </div>

      <!-- 9. CONSEQUENCES -->
      <div class="art-section" id="consequences">
        <div class="art-section-num">Section 9</div>
        <h2>If you do nothing — the consequences</h2>
        <p>Government guidance warns that if you do not submit mandatory information and pay by the deadline, you may have to pay late fees and may be subject to enforcement action.</p>
        <p>In England, civil sanctions are available under this regime. A <strong>fixed monetary penalty of £1,000</strong> (increasing by 50% if not paid within 56 days) applies to certain contraventions such as record-keeping and evidence duties. Failures to <strong>register or report data</strong> — the most common breaches — are subject to <strong>variable monetary penalties and compliance notices</strong> instead, calculated case-by-case with no fixed upper limit.</p>
        <div class="callout callout-warn">
          <div class="callout-lbl">The £1,000 figure is not the ceiling</div>
          <p>The fixed £1,000 penalty applies only to record-keeping failures. For registration and reporting failures, variable monetary penalties apply with no fixed upper limit.</p>
        </div>
      </div>

      <!-- SOURCES -->
      <div class="art-section" id="sources">
        <h2 style="font-family:'DM Sans',sans-serif;font-size:0.85rem;font-weight:600;color:var(--ink-faint);letter-spacing:0.08em;text-transform:uppercase;border:none;padding:0;margin-bottom:1rem;">Official sources used</h2>
        <ul class="sources-list">
          <li>GOV.UK — EPR for packaging: what you must do as a 'small producer'</li>
          <li>GOV.UK — Extended producer responsibility for packaging: register and pay the fee</li>
          <li>GOV.UK — Packaging data: what to collect for extended producer responsibility</li>
          <li>GOV.UK — Environment Agency RPS 330: EPR for packaging producer data requirements for 2024 and 2025</li>
          <li>GOV.UK — Packaging data: check reporting periods and submission deadlines</li>
          <li>GOV.UK — File specification for packaging data from 2025</li>
          <li>GOV.UK — Environment Agency enforcement and sanctions policy: Annex 4</li>
          <li>GOV.UK — Extended producer responsibility for packaging: who is affected and what to do</li>
        </ul>
        <p style="font-size:0.78rem;color:var(--ink-faint);margin-top:1rem;font-weight:300;">Relicare is an independent data preparation service and is not a registered compliance scheme. Always verify current fees and deadlines at the official GOV.UK Report Packaging Data service.</p>
      </div>

    </article>

    <!-- SIDEBAR -->
    <aside class="sidebar">
      <div class="sidebar-sticky">

        <!-- CTA CARD -->
        <div class="cta-card">
          <div class="section-label">Get started</div>
          <h3>Not sure if this applies to you?</h3>
          <p>Find out if you qualify as a small producer and get an instant fixed-price quote in under 2 minutes.</p>
          <a href="/eligibility-check" class="btn-primary-light" style="width:100%;text-align:center;">Check your obligation &rarr;</a>
        </div>

        <!-- TOC (sticky sidebar, mirrors hero card TOC on scroll) -->
        <div style="background:var(--white);border:1px solid var(--border);border-radius:6px;padding:1.5rem;">
          <div style="font-size:0.7rem;font-weight:600;letter-spacing:0.1em;text-transform:uppercase;color:var(--ink-faint);margin-bottom:1rem;padding-bottom:0.75rem;border-bottom:1px solid var(--border);">Jump to section</div>
          <ol class="toc-list" id="sidebar-toc">
            <li><a href="#scope"><span class="toc-num">1.</span> Are you in scope?</a></li>
            <li><a href="#thresholds"><span class="toc-num">2.</span> Small vs large producer</a></li>
            <li><a href="#activities"><span class="toc-num">3.</span> Packaging activities</a></li>
            <li><a href="#steps"><span class="toc-num">4.</span> What you must do</a></li>
            <li><a href="#rules"><span class="toc-num">5.</span> Reporting rules</a></li>
            <li><a href="#bags"><span class="toc-num">6.</span> Plastic &amp; paper bags</a></li>
            <li><a href="#fees"><span class="toc-num">7.</span> Fees</a></li>
            <li><a href="#dont"><span class="toc-num">8.</span> What you don't have to do</a></li>
            <li><a href="#consequences"><span class="toc-num">9.</span> Consequences</a></li>
          </ol>
        </div>

      </div>
    </aside>

  </div>
</div>

<!-- FINAL CTA (identical to homepage) -->
<section class="cta-section">
  <div class="container">
    <div class="section-label">Get started</div>
    <h2>Deadline-driven? So are we.</h2>
    <p>Check whether you qualify in two minutes. If you do, we confirm scope and get moving — before the deadline becomes a problem.</p>
    <div class="cta-actions">
      <a href="/eligibility-check" class="btn-primary-light">Check your reporting obligation &rarr;</a>
      <a href="/#packages" class="btn-ghost">See our packages</a>
    </div>
  </div>
</section>

<!-- FOOTER (identical to homepage) -->
<footer>
  <p>
    &copy; 2026 Relicare &mdash; UK Packaging EPR Submission Specialists<br />
    <a href="/explainer">What is EPR?</a> &middot;
    <a href="/eligibility-check">Eligibility checker</a> &middot;
    <a href="/#how-it-works">How it works</a> &middot;
    <a href="/#packages">Pricing</a>
  </p>
  <p style="margin-top:0.75rem;font-size:0.75rem;color:#2a3a30;">
    We are an independent specialist service provider. We are not a registered compliance scheme. We do not provide legal advice.
  </p>
</footer>

<!-- SCROLL SPY -->
<script>
  const ids = ['scope','thresholds','activities','steps','rules','bags','fees','dont','consequences','sources'];
  const allTocLinks = document.querySelectorAll('.toc-list a');

  const io = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        allTocLinks.forEach(l => l.classList.remove('toc-active'));
        document.querySelectorAll(`.toc-list a[href="#${e.target.id}"]`).forEach(l => l.classList.add('toc-active'));
      }
    });
  }, { rootMargin: '-15% 0px -75% 0px' });

  ids.forEach(id => {
    const el = document.getElementById(id);
    if (el) io.observe(el);
  });
</script>

</body>
</html>